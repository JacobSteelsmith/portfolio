---
import BaseLayout from '../layouts/BaseLayout.astro';

// Import the Markdown file and render it
const resumePage = await import('../content/pages/resume.md');
const { Content } = resumePage;
const { title, description } = resumePage.frontmatter;
---

<BaseLayout title={`${title} - Jacob Steelsmith`} description={description}>
  <article class="static-page">
    <Content />
  </article>
</BaseLayout>

<style>
  .static-page {
    max-width: var(--max-content-width);
  }

  .static-page :global(h1) {
    font-size: 2rem;
    margin: 0 0 1.5rem;
  }

  .static-page :global(h2) {
    font-size: 1.5rem;
    margin: 2rem 0 1rem;
    padding-top: 1rem;
    border-top: 1px solid var(--color-border);
  }

  .static-page :global(h2:first-of-type) {
    border-top: none;
    padding-top: 0;
  }

  .static-page :global(h3) {
    font-size: 1.125rem;
    margin: 1.5rem 0 0.75rem;
  }

  .static-page :global(ul) {
    padding-left: 1.5rem;
  }

  .static-page :global(li) {
    margin-bottom: 0.5rem;
  }

  .static-page :global(p) {
    margin: 0 0 1rem;
  }

  .static-page :global(em) {
    color: var(--color-text-muted);
  }
</style>
